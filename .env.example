# Absolute base URL of the deployed site. Required for Open Graph / link previews:
# without it the OG image URL resolves against localhost and previews render blank.
# Set this in the host's build environment (Vercel / Azure SWA / Cloudflare Pages).
NEXT_PUBLIC_SITE_URL=https://your-domain.com
